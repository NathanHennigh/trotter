// Metro resolves the native implementation on Android/iOS. Web deliberately
// opens Google Maps instead of displaying Google Places data on another map.
export { DreamPlacesMap } from "./DreamPlacesMap.web";

EXPO_PUBLIC_TROTTER_API_URL=https://trotter-api.thehennighs.com
# Maps SDK keys are distinct from the protected backend Places key.
# For local/WSL builds use android/google-maps.properties; see its example.
